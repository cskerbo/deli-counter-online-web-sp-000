def line(array)
  if array.length < 1
    puts "The line is currently empty."
  else
    array.each_with_index {|person, number| array}
    puts "The line is currently: #{number}. #{person}"
  end
end
